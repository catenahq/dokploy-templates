# Generated by catenahq/templates/build/render.py. Do not edit.

[variables]
OUTLINE_HOSTNAME = "wiki.${domain}"
OUTLINE_SECRET_KEY = "{{ lookup('password', '/dev/null length=64 chars=hexdigits') | lower }}"
OUTLINE_UTILS_SECRET = "{{ lookup('password', '/dev/null length=64 chars=hexdigits') | lower }}"
DB_PASSWORD = "${password:32}"

[config]

[[config.domains]]
host = "wiki.${domain}"
serviceName = "outline"
port = 3000

[config.env]
OUTLINE_HOSTNAME = "wiki.${domain}"
OUTLINE_SECRET_KEY = "{{ lookup('password', '/dev/null length=64 chars=hexdigits') | lower }}"
OUTLINE_UTILS_SECRET = "{{ lookup('password', '/dev/null length=64 chars=hexdigits') | lower }}"
DB_PASSWORD = "${password:32}"
