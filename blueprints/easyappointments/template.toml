# Generated by catenahq/templates/build/render.py. Do not edit.

[variables]
EASYAPPOINTMENTS_HOSTNAME = "book.${domain}"
DB_PASSWORD = "${password:32}"
DB_ROOT_PASSWORD = "${password:32}"

[config]

[[config.domains]]
host = "book.${domain}"
serviceName = "easyappointments"
port = 80

[config.env]
EASYAPPOINTMENTS_HOSTNAME = "book.${domain}"
DB_PASSWORD = "${password:32}"
DB_ROOT_PASSWORD = "${password:32}"
