# Generated by catenahq/templates/build/render.py. Do not edit.

[variables]
ESPOCRM_HOSTNAME = "crm.${domain}"
ESPOCRM_ADMIN_USERNAME = "admin"
ESPOCRM_ADMIN_PASSWORD = "${password:24}"
DB_PASSWORD = "${password:32}"
DB_ROOT_PASSWORD = "${password:32}"
OIDC_CLIENT_ID = "espocrm"
OIDC_CLIENT_SECRET = ""
OIDC_ISSUER_URL = "https://{{ keycloak_hostname | default('auth.' ~ cloudflare_zone) }}/realms/{{ keycloak_realm | default('vps') }}"

[config]

[[config.domains]]
host = "crm.${domain}"
serviceName = "espocrm"
port = 80

[config.env]
ESPOCRM_HOSTNAME = "crm.${domain}"
ESPOCRM_ADMIN_USERNAME = "admin"
ESPOCRM_ADMIN_PASSWORD = "${password:24}"
DB_PASSWORD = "${password:32}"
DB_ROOT_PASSWORD = "${password:32}"
OIDC_CLIENT_ID = "espocrm"
OIDC_CLIENT_SECRET = ""
OIDC_ISSUER_URL = "https://{{ keycloak_hostname | default('auth.' ~ cloudflare_zone) }}/realms/{{ keycloak_realm | default('vps') }}"
