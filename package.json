{
  "name": "@catenahq/templates",
  "version": "0.1.0",
  "private": false,
  "description": "Catena Dokploy template catalog: per-template compose files + metadata + render pipeline emitting Dokploy-marketplace-compatible blueprints. Consumed by catenahq/ops (vendored tarball) and Dokploy directly (BASE URL).",
  "type": "module",
  "files": [
    "source"
  ],
  "scripts": {
    "render": "uv run build/render.py",
    "verify": "uv run build/render.py && git diff --exit-code blueprints meta.json",
    "check:unicode": "node scripts/check-unicode.mjs"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/catenahq/templates.git"
  },
  "license": "SEE LICENSE IN LICENSE"
}
