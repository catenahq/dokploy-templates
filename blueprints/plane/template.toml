# Generated by catenahq/templates/build/render.py. Do not edit.

[variables]
PLANE_HOSTNAME = "projects.${domain}"
PLANE_SECRET_KEY = "${password:64}"
DB_PASSWORD = "${password:32}"
PLANE_MINIO_ACCESS_KEY = "${password:20}"
PLANE_MINIO_SECRET_KEY = "${password:40}"

[config]

[[config.domains]]
host = "projects.${domain}"
serviceName = "proxy"
port = 80

[config.env]
PLANE_HOSTNAME = "projects.${domain}"
PLANE_SECRET_KEY = "${password:64}"
DB_PASSWORD = "${password:32}"
PLANE_MINIO_ACCESS_KEY = "${password:20}"
PLANE_MINIO_SECRET_KEY = "${password:40}"
