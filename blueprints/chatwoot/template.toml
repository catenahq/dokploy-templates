# Generated by catenahq/templates/build/render.py. Do not edit.

[variables]
CHATWOOT_HOSTNAME = "support.${domain}"
SECRET_KEY_BASE = "{{ lookup('password', '/dev/null length=128 chars=hexdigits') | lower }}"
DB_PASSWORD = "${password:32}"

[config]

[[config.domains]]
host = "support.${domain}"
serviceName = "rails"
port = 3000

[config.env]
CHATWOOT_HOSTNAME = "support.${domain}"
SECRET_KEY_BASE = "{{ lookup('password', '/dev/null length=128 chars=hexdigits') | lower }}"
DB_PASSWORD = "${password:32}"
