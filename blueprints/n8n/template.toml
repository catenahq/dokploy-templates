# Generated by catenahq/templates/build/render.py. Do not edit.

[variables]
N8N_HOSTNAME = "automate.${domain}"
N8N_TIMEZONE = "UTC"
N8N_ENCRYPTION_KEY = "${password:64}"
N8N_JWT_SECRET = "${password:64}"
DB_PASSWORD = "${password:32}"

[config]

[[config.domains]]
host = "automate.${domain}"
serviceName = "n8n"
port = 5678

[config.env]
N8N_HOSTNAME = "automate.${domain}"
N8N_TIMEZONE = "UTC"
N8N_ENCRYPTION_KEY = "${password:64}"
N8N_JWT_SECRET = "${password:64}"
DB_PASSWORD = "${password:32}"
