# Generated by catenahq/templates/build/render.py. Do not edit.

[variables]
MAIL_HOSTNAME = "mail.${domain}"
MAIL_DOMAIN = "${domain}"
WEBMAIL_HOSTNAME = "webmail.${domain}"
OIDC_INTROSPECTION_URL = "https://{{ keycloak_hostname | default('auth.' ~ cloudflare_zone) }}/realms/{{ keycloak_realm | default('vps') }}/protocol/openid-connect/token/introspect"
RELAY_HOST = ""
RELAY_PORT = "587"
RELAY_USER = ""
RELAY_PASSWORD = "__CATENA_OPERATOR_WIRED__"

[config]

[[config.domains]]
host = "webmail.${domain}"
serviceName = "roundcube"
port = 80

[config.env]
MAIL_HOSTNAME = "mail.${domain}"
MAIL_DOMAIN = "${domain}"
WEBMAIL_HOSTNAME = "webmail.${domain}"
OIDC_INTROSPECTION_URL = "https://{{ keycloak_hostname | default('auth.' ~ cloudflare_zone) }}/realms/{{ keycloak_realm | default('vps') }}/protocol/openid-connect/token/introspect"
RELAY_HOST = ""
RELAY_PORT = "587"
RELAY_USER = ""
RELAY_PASSWORD = "__CATENA_OPERATOR_WIRED__"
