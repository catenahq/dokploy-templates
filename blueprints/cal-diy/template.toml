# Generated by catenahq/templates/build/render.py. Do not edit.

[variables]
CALDIY_HOSTNAME = "cal.${domain}"
NEXTAUTH_SECRET = "${password:64}"
CALENDSO_ENCRYPTION_KEY = "${password:32}"
DB_PASSWORD = "${password:32}"

[config]

[[config.domains]]
host = "cal.${domain}"
serviceName = "caldiy"
port = 3000

[config.env]
CALDIY_HOSTNAME = "cal.${domain}"
NEXTAUTH_SECRET = "${password:64}"
CALENDSO_ENCRYPTION_KEY = "${password:32}"
DB_PASSWORD = "${password:32}"
