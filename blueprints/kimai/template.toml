# Generated by catenahq/templates/build/render.py. Do not edit.

[variables]
KIMAI_HOSTNAME = "time.${domain}"
KIMAI_ADMIN_EMAIL = "admin@${domain}"
KIMAI_ADMIN_PASSWORD = "${password:24}"
KIMAI_APP_SECRET = "${password:64}"
DB_PASSWORD = "${password:32}"
DB_ROOT_PASSWORD = "${password:32}"
KIMAI_MAIL_FROM = "time@${domain}"
KIMAI_MAIL_URL = "null://localhost"

[config]

[[config.domains]]
host = "time.${domain}"
serviceName = "kimai"
port = 8001

[config.env]
KIMAI_HOSTNAME = "time.${domain}"
KIMAI_ADMIN_EMAIL = "admin@${domain}"
KIMAI_ADMIN_PASSWORD = "${password:24}"
KIMAI_APP_SECRET = "${password:64}"
DB_PASSWORD = "${password:32}"
DB_ROOT_PASSWORD = "${password:32}"
KIMAI_MAIL_FROM = "time@${domain}"
KIMAI_MAIL_URL = "null://localhost"
